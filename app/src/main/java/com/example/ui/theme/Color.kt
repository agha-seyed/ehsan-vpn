package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Frosted Glass Mallard Teal (کله‌غازی) Dark Canvas Palette
val DarkBg = Color(0xFF061415)      // Luxurious deep dark duck-neck teal
val SurfaceDark = Color(0xFF0D2527)  // Medium rich teal container base
val SurfaceCard = Color(0x12FFFFFF)  // Semi-transparent frosted glass card base with light teal hint

// Crisp borders for Glassmorphism
val GlassBorder = Color(0x22E5C158)        // Radiant soft gold border
val GlassBorderStrong = Color(0x44E5C158)  // Strong gold border for high focus UI
val GlassGlowTeal = Color(0x1A00F2D1)      // Gorgeous glowing teal aura
val GlassGlowGold = Color(0x1AE5C158)      // Rich radiating gold aura

// Neon & Luxury Accents
val CyanGlow = Color(0xFF00F2D1)     // Electric teal-cyan glow (Mallard-feather eye)
val CobaltBlue = Color(0xFFE5C158)   // Splendid Imperial Gold
val CyberGreen = Color(0xFF0DFFA1)   // Energetic secure connection green
val AlertRed = Color(0xFFFF4B72)     // Warning alert accent red

// Text Colors
val TextPrimary = Color(0xFFE5ECEC)    // Teal-tinted elegant white
val TextSecondary = Color(0xFF92A9AA)  // Duck-egg slate gray-teal
val TextMuted = Color(0xFF5B7475)      // Deep muted teal for guidance

// Premium light color scheme fallback
val PrimaryBlue = Color(0xFFE5C158)    // Gold Primary
val SecondaryBlue = Color(0xFF008080)  // Teal Secondary
